<apex:page standardController="contact" recordSetVar="contacts">
  <apex:dataTable var="c" value="{!contacts}">
  <apex:column value="{!c.firstname}"/>
  <apex:column value="{!c.Lastname}"/>
  <apex:column value="{!c.Email}"/>
  </apex:dataTable>
</apex:page>